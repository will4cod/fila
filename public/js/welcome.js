$(document).ready(() => {
    $(".btnRegistrar").click(() => {
        window.location="/usuario/registrar"
    })
})

$(document).ready(() => {
    $(".btnLogin").click(() => {
        window.location="/usuario/login"
    })
})
